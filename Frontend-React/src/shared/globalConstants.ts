export const GlobalConstants = {
  genericError: 'Something went wrong. pleases try again later',

  nameRegex: /^[a-zA-Z0-9 ]*$/,
  emailRegex: /^[A-Za-z0-9._%-]+@[A-Za-z0-9._%-]+\.[a-z]{2,3}$/,
  contactNumberRegex: /^[0-9]{10,10}$/,

  productExistError: 'Product already exists',
  productAdded: 'Product added successfully',
  unauthroized: 'You are not authorized person to access this page.',

  error: 'error',
};
